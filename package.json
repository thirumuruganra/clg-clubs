{
  "name": "clg-clubs",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-postbuild": "npm --prefix frontend install && npm --prefix frontend run build && rm -rf backend/app/static && mkdir -p backend/app/static && cp -r frontend/dist/. backend/app/static/"
  }
}
